export interface TargetSavingModel {
    targetSavingId : number,
    idUser : string,
    startDate : Date,
    endDate : Date,
    targetAmount : number
    stateSaving : string,
    isDeleted : boolean
}